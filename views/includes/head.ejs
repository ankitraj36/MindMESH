<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>MindMesh | Inspire Minds</title>
  <link rel="icon" href="/images/logo.png" sizes="32x32" type="image/jpeg">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <style>
    :root {
      --bg-light: #f8f9fa;
      --bg-dark: #121212;
      --text-light: #212529;
      --text-dark: #f1f1f1;
      --card-light: #ffffff;
      --card-dark: #1e1e1e;
      --content-light: #343a40;
      --content-dark: #e1e1e1;
    }

    body {
      background: var(--bg-light);
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      padding-top: 70px;
      margin: 0;
      min-height: 100vh;
      color: var(--text-light);
      transition: all 0.3s ease-in-out;
    }

    body.dark-mode {
      background: var(--bg-dark);
      color: var(--text-dark);
    }

    .container {
      max-width: 850px;
      margin: auto;
      padding: 0 15px;
    }

    .navbar {
      background-color: #5f27cd;
    }

    .navbar-brand img {
      width: 30px;
      height: 30px;
      object-fit: cover;
    }

    h1 {
      font-size: 2.5rem;
      color: #5f27cd;
      font-weight: 800;
      text-align: center;
      margin-bottom: 40px;
    }

    .card {
      background: var(--card-light);
      border-radius: 16px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.08);
      margin-bottom: 25px;
      transition: all 0.3s ease-in-out;
      border: none;
    }

    body.dark-mode .card {
      background: var(--card-dark);
    }

    .card:hover {
      transform: translateY(-6px);
      box-shadow: 0 12px 28px rgba(0, 0, 0, 0.15);
    }

    .username {
      font-style: italic;
      font-weight: 600;
      color: #1d9bf0;
      font-size: 1.1rem;
    }

    .content {
      font-size: 1.15rem;
      line-height: 1.6;
      color: var(--content-light);
      transition: color 0.3s ease-in-out;
    }

    body.dark-mode .content {
      color: var(--content-dark);
    }

    .btn-action {
      margin-right: 10px;
      transition: transform 0.2s ease-in-out;
    }

    .btn-action:hover {
      transform: scale(1.05);
    }

    .comment-form {
      margin-top: 15px;
    }

    .comment-list {
      margin-top: 15px;
      padding-left: 5px;
    }

    .comment-item {
      font-size: 0.95rem;
      padding: 6px 0;
      border-bottom: 1px solid #e9ecef;
    }

    .btn-floating {
      position: fixed;
      bottom: 30px;
      right: 30px;
      background: linear-gradient(to right, #ff416c, #ff4b2b);
      color: #fff;
      border-radius: 50%;
      font-size: 24px;
      padding: 15px 20px;
      box-shadow: 0 4px 16px rgba(0,0,0,0.2);
      z-index: 999;
    }

    .btn-floating:hover {
      background: linear-gradient(to right, #ff4b2b, #ff416c);
    }

    .theme-toggle {
      position: fixed;
      top: 15px;
      right: 20px;
      z-index: 1000;
    }

    @media (max-width: 576px) {
      h1 { font-size: 2rem; }
      .card { padding: 15px; }
      .content { font-size: 1rem; }
    }

    .no-underline {
  text-decoration: none;
  color: #0d6efd; /* Bootstrap primary blue */
  font-weight: 500;
  display: inline-flex;
  align-items: center;
  gap: 5px;
}

.no-underline:hover {
  color: #084298;
  text-decoration: none;
}

  </style>
</head>